import React from 'react';

// need to build the rightasidebar but first
// look at the design and see what is needed how would you have made it
// and then do v0 of it to check if you are right
// and then come to the actual code to check for differences

const RightSidebar = () => {
  return <div>RightSidebar</div>;
};

export default RightSidebar;
